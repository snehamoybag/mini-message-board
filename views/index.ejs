<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Mini Message Board</title>
  </head>
  <body>
    <main>
      <section><h2><%= title %></h2></section>
      <ul>
        <% messages.forEach((message) => { %>
        <li>
          <h3><%= message.user %></h3>
          <p><%= message.text %></p>
          <p>on: <%= message.name %> (<%= message.added %>)</p>
        </li>
        <% }); %>
      </ul>
    </main>
  </body>
</html>
